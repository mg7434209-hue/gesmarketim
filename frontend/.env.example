# Frontend env (Vite reads VITE_* vars at build/dev time)

# Base URL of the backend API.
# In dev, Vite proxies /api → this URL so you can fetch('/api/...') directly.
VITE_API_URL=http://localhost:3000
